<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Engine.WpfControl"
                    xmlns:wb="clr-namespace:Engine.WpfBase">
    <Style x:Key="S.ToggleExpander.Default" TargetType="{x:Type local:ToggleExpander}">
        <Setter Property="Foreground" Value="{DynamicResource S.Brush.TextForeground.Default}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="wb:Cattach.FIconSize" Value="60"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <!--<Setter Property="Visibility" Value="Collapsed"/>-->
                    <Setter Property="RenderTransform" Value="{StaticResource S.TransformGroup.Default}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel IsItemsHost="True" VerticalAlignment="Center" Orientation="Vertical" HorizontalAlignment="Left"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ItemsControl">
                    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <ToggleButton x:Name="tg" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                      Content="&#xe639;" wb:Cattach.FIcon="&#xe635;"
                                      wb:Cattach.FIconSize="{TemplateBinding wb:Cattach.FIconSize}"  
                                      wb:InvokeTimeSplitAnimationAction.IsExcept="True" 
                                      Style="{DynamicResource S.ToggleButton.DoubleFIconChecked}">
                                <!--<wb:Interaction.Triggers>
                                    <wb:EventTrigger EventName="MouseEnter">
                                        <wb:ChangePropertyAction PropertyName="IsChecked" Value="true"/>
                                    </wb:EventTrigger>
                                </wb:Interaction.Triggers>-->
                            </ToggleButton>

                            <Popup x:Name="PART_Popup" AllowsTransparency="True" VerticalOffset="2" IsOpen="{Binding ElementName=tg,Path=IsChecked}"
                               PlacementTarget="{Binding ElementName=tg}" StaysOpen="True"
                               PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">

                                <Grid>
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            CornerRadius="{DynamicResource S.Window.Item.CornerRadius}"/>

                                    <!--<Polygon  HorizontalAlignment="Left" Fill="{DynamicResource S.Brush.Accent}" 
                                              Stroke="{DynamicResource S.Brush.TextBorderBrush.Default}" 
                                              Points="0,5 5,0 10,5 0,5"
                                              Margin="0,-5,8,0" FillRule="Nonzero"/>-->

                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                </Grid>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity"  Value="{DynamicResource S.Opacity.Disable}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="S.ToggleExpander.Single" TargetType="{x:Type local:ToggleExpander}" BasedOn="{StaticResource S.ToggleExpander.Default}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Background" Value="{DynamicResource S.Brush.Accent}"/>
        <Setter Property="Foreground" Value="{DynamicResource S.Brush.TextForeground.White}"/>
        <Setter Property="wb:Cattach.CornerRadius" Value="28"/>
        <Setter Property="wb:Cattach.FIconSize" Value="40"/>
        <Setter Property="DockPanel.Dock" Value="Top"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ToggleExpander}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="{TemplateBinding wb:Cattach.CornerRadius}">
                        <DockPanel HorizontalAlignment="Left" VerticalAlignment="Center">

                            <ToggleButton x:Name="tg" HorizontalAlignment="Center"  wb:InvokeTimeSplitAnimationAction.IsExcept="True" VerticalAlignment="Center" DockPanel.Dock="{TemplateBinding DockPanel.Dock}"
                                      Content="&#xe639;" wb:Cattach.FIcon="&#xe635;"   
                                      IsChecked="{TemplateBinding IsExpanded}"
                                      Foreground="{TemplateBinding Foreground}" Margin="5,0"
                                      wb:Cattach.FIconSize="{TemplateBinding wb:Cattach.FIconSize}"  
                                      Style="{DynamicResource S.ToggleButton.DoubleFIconChecked}">
                                <wb:Interaction.Triggers>
                                    <wb:EventTrigger EventName="Checked">
                                        <wb:InvokeCommandAction Command="{x:Static wb:CommandService.VisibleSplitAnimationCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                    </wb:EventTrigger>

                                    <wb:EventTrigger EventName="Unchecked">
                                        <wb:InvokeCommandAction Command="{x:Static wb:CommandService.CollapsedSplitAnimationCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                    </wb:EventTrigger>
                                </wb:Interaction.Triggers>
                            </ToggleButton>

                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity"  Value="{DynamicResource S.Opacity.Disable}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="IsChecked"  Value="true" TargetName="tg"/>
                            <Setter Property="Cursor" Value="ScrollAll"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="S.ToggleExpander.Center" TargetType="{x:Type local:ToggleExpander}" BasedOn="{StaticResource S.ToggleExpander.Single}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="wb:Cattach.FIconSize" Value="50"/>
        <Setter Property="wb:Cattach.CornerRadius" Value="250"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ToggleExpander">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding wb:Cattach.CornerRadius}">
                        <Grid>
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>

                            <ToggleButton x:Name="tg" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                          DockPanel.Dock="{TemplateBinding DockPanel.Dock}"
                                          Content="&#xe639;" wb:Cattach.FIcon="&#xe635;"
                                          IsChecked="{TemplateBinding IsExpanded}"
                                           wb:InvokeTimeSplitAnimationAction.IsExcept="True" 
                                          wb:Cattach.FIconSize="{TemplateBinding wb:Cattach.FIconSize}"  
                                          Style="{DynamicResource S.ToggleButton.DoubleFIconChecked}">
                                <wb:Interaction.Triggers>
                                    <wb:EventTrigger EventName="Checked">
                                        <wb:InvokeCommandAction Command="{x:Static wb:CommandService.VisibleSplitAnimationCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                    </wb:EventTrigger>

                                    <wb:EventTrigger EventName="Unchecked">
                                        <wb:InvokeCommandAction Command="{x:Static wb:CommandService.CollapsedSplitAnimationCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                    </wb:EventTrigger>
                                </wb:Interaction.Triggers>
                            </ToggleButton>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity"  Value="{DynamicResource S.Opacity.Disable}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="IsChecked"  Value="true" TargetName="tg"/>
                            <Setter Property="Cursor" Value="ScrollAll"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <local:CirclePanel Len="60" Margin="25" IsAnimation="False"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:ToggleExpander}" BasedOn="{StaticResource S.ToggleExpander.Default}"/>


    <Style x:Key="S.ElementExpander.MouseEnter" TargetType="{x:Type local:ElementExpander}" BasedOn="{StaticResource S.ToggleExpander.Single}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="wb:Cattach.FIconSize" Value="50"/>
        <Setter Property="wb:Cattach.CornerRadius" Value="250"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ElementExpander">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding wb:Cattach.CornerRadius}">
                        <Grid>
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                            <ContentControl Content="{TemplateBinding Content}"/>
                        </Grid>

                        <wb:Interaction.Triggers>
                            <wb:EventTrigger EventName="MouseEnter">
                                <wb:InvokeCommandAction Command="{x:Static wb:CommandService.VisibleSplitAnimationCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                            </wb:EventTrigger>

                            <wb:EventTrigger EventName="MouseLeave">
                                <wb:InvokeCommandAction Command="{x:Static wb:CommandService.CollapsedSplitAnimationCommand}"  CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                            </wb:EventTrigger>
                        </wb:Interaction.Triggers>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity"  Value="{DynamicResource S.Opacity.Disable}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <local:CirclePanel Len="60" Margin="25" IsAnimation="False"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

